name: Plugin ZIP signal
on:
  push:
    branches: ['**']
permissions: {}
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Public refs changed; trusted default-branch inventory will scan them.'
