# 設定値の全文表示とポップアップの配置（2026-09-06）

#27 / #20、MV-042。ユーザーが送付した設定パネルとCursorのモデル/モード展開位置・Agent背景の4画像に対応。
GPTが実装/GUI、Claude Proが独立レビュー。45分・修正3回・送信0回の予算で2コミット/2ビルド（r1未インストール）。

## 変更

- チャット設定の項目名を左に固定し、残りの横幅を現在値へ割当て、右揃えで表示。早い段階で「CLIの…」「このプロジェ…」となっていた余白配分を修正。
- 項目名を「このセッションの内容を要約」「MCPサーバー設定」に変更。
- SelectorButtonのラベル配置には、幅計算と同じコンポーネントのFontMetricsを使用。
- モデル/モードはIDE標準 `PopupShowOptions.aboveComponent` により、ボタン上に4の間隔を空けて開く。モデルの検索・Autoによる高さ変更は下端をボタン上に揃え直す。スクリーンの左右・上端を考慮する。
- `SelectorPopupController`が開いている1つのpopupを管理。再クリックで閉じ、`PopupUtil.setPopupToggleComponent`で外側クリックによる閉じる/再び開く競合を防止。外側へのフォーカス移動、他ウィンドウ、Escapeでも閉じる。通常終了・IDEの直接disposeの両方でグローバルfocus listenerを解除。
- Agent背景をdark `#383838` / light `#E3E3E3`へ変更し、常時グレーのピルを表示。

CLI・権限の既定値・保存値・会話送信には変更なし。Debug/Multitaskは引き続き未対応。

## ビルドと実際の反映

- 実装 `3b8e229`、dispose補強 `b26479129dbacf77b44a048311a958e6c5c4eee3`
- 最終run `.loop-runs/20260906-popup-ui-r2`
- ZIP SHA-256 `18402007d3cf1e7997cb124b8c830a21a109c095c4c111759a9fd698f186a173`
- plugin JAR SHA-256 `54f20631d0479bf4c7ac2f2ce49631388260305e355107b3b692467698f164ec`
- Android Studio `AI-261.26222.65.2614.16204760`
- 実際のfixture `.loop-runs/20260906-issue20-r3/plugin`

04:04の通常終了後、listAppsは停止を返したが、04:05に別の再起動が行われていた。04:06の配置後にこの起動ログと旧背景を検出したため、初回captureを更新版の合格証拠として扱っていない。04:07:22に再度正常終了し、停止・配置済みJARハッシュを確認して再起動。以降の `loaded-composer.png` で新しいグレー背景を確認した。バックアップと全JAR照合、起動ログはrun内evidence/install.txt・startup.txtに保存。

## 観察と残るGUI確認

MV-042は **blocked（部分表示確認のみ）**。

- 最終版の起動、Agentグレー背景、空入力、Agent / Composer 2.5を確認。
- ボタンのAXクリックでフォーカスは取得できたが、メニューの画面を取得できなかった。Return/Spaceの確認中にScreenCaptureKit -3811、座標クリックはnoWindowsAvailable。接続初期化、最新AX/スクリーンショット照合、再起動を試した。
- 設定値全文・指定ラベル・上方向展開・同じボタンで取消・外側フォーカスで終了・Auto/検索時の位置維持は、今回の実機では未確認。自動テストをGUI合格の代用にしない。
- 入力送信と設定変更は0。最後の確認状態は空入力・Agent / Composer 2.5。

## 自動検証とレビュー採否

63テスト、`test buildPlugin`成功。追加テストで350/420幅の設定値が空き領域を使うこと、指定ラベル、Auto相当の高さ変更でpopup下端がボタン上に残ること、同じtriggerの2回目が再生成しないこと、外側focus/直接disposeでlistenerが残らないことを検証。

Claude Pro（claude.ai契約、read-only CLI）の独立レビューを取得。

- show失敗時のcleanup重複を省略する提案は不採用。show失敗はonClosedに到達する保証がなく、冪等の明示cleanupを残した。追加でDisposerにもcleanupを登録し、IDEの直接破棄をテストした。
- pack直後の位置補正によるちらつき、beforeShownでの選択行スクロールはGUI確認対象として保持。同期EDT処理であり、ソースだけでちらつきの発生を断定しない。
- 設定値の右寄せ/API importの疑義は、SelectorButtonの実装とSDK照合・ビルドで確認済み。
- 再クリック競合の抑止は、対象SDKのStackingPopupDispatcherImplがtoggle component上のマウスイベント処理を停止してcancelすることもローカルbytecodeで確認。

コードは統合するが、MV-042の操作QAと#27/#20全体はopenを維持。次は最終インストール済みb264791で、操作ツール復帰後に未確認ケースを実行する。再配置は不要。
