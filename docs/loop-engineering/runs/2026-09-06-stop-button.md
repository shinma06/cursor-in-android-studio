# STOP-BUTTON-1 — 応答停止ボタン

Issue #69 / parent #19 / run 20260906-stop-button-r1。

## 変更

24pxの送信ボタンに対してSelectorButtonの左右6pxの余白があり、停止文字の
描画領域は12pxしかなかった。文字はSwingの省略対象になるため、処理中は
18pxの円と中央7pxの角丸四角を描くStopIconへ変更。余白は四辺3pxとする。
明暗テーマで配色を切り替え、JBUIの表示倍率に追従する。
送信/停止callback、入力無効中の停止ボタン有効状態、tooltip/accessibilityを維持する。

## 固定ビルドの確認手順（未実施）

GUI担当はhost lease取得後、PR記載のHEAD/base/ZIP hashとロードJARを照合する。
使い捨てfixtureでPro/Teamsアカウント、Askモードを使う。最大15分、2送信。

1. 待機中の上矢印と「送信（Enter）」tooltipを確認する。
2. 「1から100までの数字を1行ずつ出力してください。ファイル操作とコマンド実行は不要です。」
   を送信。実行中に明るい円と暗い中央四角が表示され、省略記号や切れがないことを確認。
3. 入力が無効でも停止ボタンを押せ、応答が止まり、上矢印へ戻ることを確認。
   次の短い応答が通常完了しても上矢印へ戻ることを確認。
4. 通常幅/約320px、明暗テーマ、hover/Tab focusで形が欠けず、停止操作が可能なことを確認。
   明テーマは暗い円・白い四角。倍率変更が可能なら拡大時も確認する。
5. 元のmode/model/テーマ/サイズへ戻し、実行停止・入力空・dialog閉鎖を確認する。

## 証拠・状態

GUI: queued / 未確認。既存の人間GUI予約があるため、このwriterはデスクトップを操作しない。
ビルド・テストとGUIの観察を区別し、実機passは固定HEADの証拠取得後に記録する。
HEAD/base、ZIP SHA-256、テスト結果はIssue/PRの引継ぎコメントを正本とする。
