# トークン数パネルの文字階層（#71）

Case: TOKEN-PANEL-HIERARCHY-1。Owner: gpt-token-panel-20260906。
ユーザー提供の2026-09-06 15:41画像で、全テキストが同じサイズ・灰色になっていた問題の改善。

## 変更と確認範囲

タイトルは通常文字より少し大きい太字とテーマ標準の前景色、数値は標準前景色の太字。
項目名は既存の控えめな色、直近の応答は小さな補足とし、その下に余白を置く。
入力/出力とキャッシュ項目の両方が存在するときだけ間に線と余白を入れる。
欠損行・全欠損時の扱い、0、数値の桁区切り、更新と開閉は既存仕様を保つ。

## ローカルの静的確認

`./gradlew test buildPlugin` 成功。既存のContextUsageTestで欠損/0/reset/開閉/世代とボタン描画を確認。
実装クラスをheadless JavaでBufferedImageへ描画し、280/350px、明暗の模擬色、全値/出力0のみ/空の12枚を生成。
全値の通常暗色/狭幅明色、一部値の狭幅暗色、空の狭幅明色を目視し、階層・区切り・値の保持と非重複を確認。
使用値はユーザー画像と同じ28,791 / 141 / 5,748 / 0を渡した合成データ。実CLI送信ではない。
描画用フォントとテーマはfixtureで指定したものなので、IDEの実テーマや実機GUI合格とは区別する。
画像と再現用Java: `/Users/shinma/Dev/Soft/GUI-checks/token-panel-hierarchy/`。

## 固定buildのGUI依頼（未実施）

Host: local / shinma。対象: Android Studio、使い捨てfixture。
Source HEAD/base/ZIP SHA-256はIssue/PRの引継ぎコメントへ記録する。
操作担当は中央進行役がGUI lease取得後に割り当てる。予算20分、CLI送信0回。
既存のトークン値または合成CLI fixtureを使用し、合成値ならその旨を証拠へ記録する。

1. 識別したZIP/JARをロードし、タイトルが太字、補足が小さく、ラベルより数値が強く見えることを確認。
2. 通常幅と約280〜320px、明暗テーマでラベル・右端数値・×が重ならないことを確認。
3. 全値は入力/出力の下に区切り、一部値は不要な区切りや空行なし、全欠損時は短い案内のみ。
4. 同じ明細ボタン/×/Spaceの開閉と、開き直した数値保持、入力focus移動で保持を確認。
5. 終了時に入力空・処理停止・dialog閉鎖、元のテーマ/サイズへ戻し、対象HEADと観察内容を記録。

現時点でインストール・runIde・CLI送信・desktop操作は実施していない。
GUI passをこの静的確認から推定せず、独立レビュー・CI・実機確認を自動進行役へ引き継ぐ。
