# 最小Swing Computer Use調査 — #80 / #73 段階1

## 結論と範囲

2026-09-08のdevelop移行: PR #82の必要Caseは
[SWING-PROBE-REPORT-1](../../verification/changes/issue-80.json)（資材・手順・報告の整合）。
全面CUA操作成立は[親#73の別Case](../../verification/changes/issue-73.json)として追跡する。
以下は旧固定sourceでの調査記録であり、移行時の新GUI検証は行っていない。
新しい固定候補のCaseはpendingを維持し、develop統合だけでIssueをcloseしない。

**調査は完了。全面的なGUI操作成立は未達。** 同じJARをjpackageの.appで起動すると
アプリ識別・標準入力・再起動後の再識別が成立した。標準Swingのmodalでは、開くイベントが
届いているのにComputer Useが背面の親窓を返す問題を再現した。製品コードやIntelliJを
含まないため、この再現に製品のSettings実装は必要ない。ただし#76と同じ内部原因かは未確定。

対象は [#80](https://github.com/shinma06/cursor-in-android-studio/issues/80)、親は
[#73](https://github.com/shinma06/cursor-in-android-studio/issues/73)。
[実測結果コメント](https://github.com/shinma06/cursor-in-android-studio/issues/80#issuecomment-5562668110)。
製品UI全体を分離/複製せず、`scripts/gui-fixture/` のJDKだけの検証画面で調査した。
#29の編集/Diff/Revert、#76実Settings、#74の実CLI異常終了、製品IME/D&Dの受入は残る。

## 固定資材・終了状態

- run: `swing-cua-r1` / observer: `gpt-80-swing-20260907`
- source: `214d6ad8d1ddae914034a4d804da6a64434a24f6`
- base: `7eb2b9d446d2f2cac21fff293c076358fc667721`
- plainと.app内のJAR SHA-256: `2e00a23dc9019a83c096a67e53b9234dd5fd141c3dd62781845b7cbbb7792376`
- fixture専用bundle ID: `com.cursoragent.fixture.swingcua`
- JDK: JBR 21.0.11、`javac 21.0.11`。jpackageが生成したnative launcher/runtimeを使用。
- 起動識別: plain / app初回 / app再起動に異なるboot IDを確認。
  アプリ自身がロード元JARのSHA-256を計算して表示/出力し、上記資材と照合。
- 実操作・停止まで約526秒（予約20分内）、Cursor CLI送信0回。
  指定GUI担当がGitHub引継ぎとfree leaseを確認して取得/開始投稿後に実行し、結果投稿後に解放。
- 自分の記録PID（plain 1 / .app 2起動）だけをSIGTERMし、全ての停止を確認。
  IDE/CLIは起動せず、IDE/プラグイン設定とOS権限は変更していない。heartbeatも再開していない。

経路A/Bは同じJARだが、.appではbundle情報だけでなくlauncher/runtime/起動経路も変わる。
成功理由をbundle ID単独へ帰属させない。再起動回復も原因の特定とは区別する。
初回.app接続のツールwall timeは245.8秒。承認待ちと実処理の内訳は不明で、
キャプチャ処理に246秒かかったとは断定しない。その後の再接続は約0.23秒だった。

## 操作別の前後と判定

| ケース | 操作前 → 公開API操作 | 操作後・補助イベント | 判定 |
|---|---|---|---|
| A: 素Java識別 | READYログ → listApps/getApp表示名 → 一覧に返ったID | 一覧にSwingCuaFixture、両getAppは即 `Invalid app` | blocked: app解決 |
| B: .app識別 | 同じJARを.app起動 → 固有IDのgetApp | build/run/bootと標準部品のAXを取得 | pass |
| B: AXクリック | 待機 → JButtonをAX click | `クリック: 1` と `CLICK 1` | pass |
| B: キー入力 | 空欄 → click入力/typeText("probe-123")/Return | 入力値と `確定: probe-123`、`COMMIT probe-123` | pass |
| B: AX値設定 | 既存入力 → setValue("値-456")/Return | 入力値と `確定: 値-456`、一致するCOMMIT | pass。IME変換の証拠ではない |
| B: Popup取得 | 通常窓 → AX click Popup | `POPUP_OPEN requested`、AXは変化なし・画像は親窓だけ | blocked: popup可視性 |
| B: Popup座標 | 上記画像でPopup位置を確認 → 座標click | `noWindowsAvailable` | blocked: 窓選択/座標経路 |
| B: Popupキー回復 | Escape → 最新AX → click Popup/Down/Return | `Popup: 選択 A` と `POPUP_SELECT 選択 A` | pass: 結果確定。展開画像passではない |
| B: scroll API | 通常窓・画像取得成功 → AX scroll、座標scroll各1回 | どちらも `noWindowsAvailable`、SCROLL変化なし | blocked |
| B: 一覧キー対照 | AX click一覧は `cannotClickOffscreenElement` → Tabでfocus/End | scrollbar 2238、末尾87〜100行の画像、`SCROLL 2238` | pass: キーscroll。scroll APIとは別 |
| B: 一覧選択 | End後 → Down | 行001を選択して先頭へ戻る、`ROW_SELECT 行 001` | 観察記録。末尾行選択成功にはしない |
| B: Modal | 通常窓 → AX click Dialog | `DIALOG_OPEN requested`。AX rootのroleはダイアログだがタイトル/子要素/画像は親窓。子の入力欄/確定ボタンなし | blocked: 前面窓誤取得 |
| B: Modal再取得 | 上記状態 → 同じIDでgetApp | 親窓のまま。子のCOMMITなし | 回復失敗、入力しない |
| C: 再起動/再識別 | Bを停止確認 → 同じ.app起動/getApp | 新boot、同じbuild/run/JAR、待機 → AX clickで1 | pass |
| C: 座標click | Popup未使用・最新画像 → 座標click | `クリック: 2` と `CLICK 2` | pass: 再起動後に回復 |
| C: scroll API | 通常窓 → AX scroll down 2 | scrollbar 1898、行74以降の画像、`SCROLL 1898` | pass: 再起動後に回復 |
| C: Popup再比較 | 最新AX → click Popup | `POPUP_OPEN requested`。AXにはメニュー文字列A/B、画像は `Screenshot unavailable` | partial: menu AXのみ |
| 終了 | CのPopup → Escape/AX保存 → 自分のPID停止 | AXは窓名のみ、全記録PID停止、lease解放 | cleanup完了 |

IME変換中/確定、D&D、非modal別窓、clipboard pasteは今回未試行。
標準text fieldの日本語直接設定をIME対応と読み替えない。
再起動後の座標/scroll成功により「この環境では座標APIが常に使えない」は反証された。
初回の通常窓で画像は取得できたため「全画面取得が一律拒否される」も今回の説明として不十分。
一方、popup/modalが最初の座標不調を引き起こしたかは、初回popup前の座標対照がなく未確定。

## 失敗層と復旧・打切り手順

| 層 | 根拠・未確定点 | 自動で進められる復旧と停止条件 |
|---|---|---|
| cwd/Node起動前 | #76の旧cwd消失・symlink writable rootは既知の別障害。今回の実体cwdではCUA初期化成功 | 起動前にcwd存在/実体パスを確認。不存在やsymlink拒否は新実体cwdのタスクへ引継ぐ。GUI再試行やIDE設定変更で直そうとしない |
| アプリ識別 | 素Javaは一覧検出とgetApp解決が一致しない。.appは解決可能 | 一覧のIDを1回試す。独立.app＋固有ID＋同一JAR比較を使う。失敗が続けばアプリ解決caseだけblocked |
| AX操作/入力 | 標準button、typeText+Return、setValue+Returnは成功 | 最新AXで対象を識別し、実際の値/結果を毎回確認。キー経路を1案比較。API成功の返却だけで入力成立としない |
| window/画像/座標 | modalの親窓誤取得、popup画像欠落。再起動前後で座標/scroll成否が変わる | 最新AX/画像から対象を確認。再取得は1回だけ比較し、同じ親窓なら打切る。使い捨てfixtureに限り自分のPIDを終了/再起動し、新bootと固定hashで再識別。これが今回成立した復旧。IDEを無断再起動しない |
| 権限・capture内部 | 標準AX/画像/座標の成功例がある。権限不足という単一説明は支持できないが、OS内部原因は未特定 | 拒否が明示された場合だけその境界と必要権限をPMへ記録。権限を勝手に変更せず、エラーコードから原因を推測しない |
| clipboard/IME | #76のpaste timeout等は既知だが、このrunではpaste未使用 | typeText/setValueとpasteを混同しない。必要なら別の限定runでclipboard復元・IMEイベントを含む対照試験 |

PMのインストール済みruntime読取によると、native `getAXState` / `getScreenshot` /
`getAXStateAndScreenshot` は同じ `get_app_state` へ到達し、getApp再取得も同じ対象でfull stateを
要求する。公開APIにはwindow ID選択がない。従ってAX→画像の切替を独立したcapture経路の
回避策に数えない。非公開APIの呼出しは行っていない。
PMが過去ログで確認した `ScreenCaptureKit -3811` / AccessibilitySupportのCode=2は背景情報であり、
今回の各失敗にそのコードが出たとは記録しない。版番号差だけでも不具合と断定しない。

## 次の小さい解決策

1. 実体cwd preflightと固有IDの.appを基本起動口にし、固定JAR/run/bootを取得できることを入口条件にする。
2. AX操作/キー＋結果確認を通常窓の検証経路にする。Popupキー選択は結果のみ検証可能で、展開表示や座標選択の受入は残す。
3. 画像/座標失敗時は別仮説の復旧1回（今回なら使い捨てfixture再起動）まで。そのcaseだけ保留し、全製品検証を人間手動QAへ移して完了にしない。
4. #73段階2はPMが範囲を決める条件付きpilotとする。同じ製品部品を同一窓へ載せる最小範囲に限り、別実装の模倣UIを作らない。modal、製品popup、実IDE/実CLI依存の合格を前提にしない。
5. 下記最小再現を上流報告用にレビューする。この作業では上流へ送信しない。

## 上流不具合報告草案（未送信）

**題:** Packaged minimal Swing app: modal capture returns its parent; popup capture and coordinate input vary across restarts

**最小構成:** JDK 21の標準Swingのみ。1 JFrameにJButton/JTextField/JList/JPopupMenu、
JButtonから所有者付き `new JDialog(frame, title, true)` を開く。jpackage app-imageで固有bundle ID。
IntelliJ、CLI、ネットワーク、ログイン、ユーザー設定への依存なし。
fixture sourceとbuild手順は本PRの `scripts/gui-fixture/`。

**再現:** 専用lease内で.appを起動 → 公開getApp(bundle ID) → 最新AXのクリックボタンを押し
カウンターを確認 → DialogボタンをAX click → getAXState/getScreenshot → getApp再取得。

**期待:** 子Dialogのタイトル・入力欄・確定ボタンと前面画像が得られる。

**実際:** fixtureはDIALOG_OPENを出す。返るAXはroot roleがダイアログへ変わる一方、
親JFrameのタイトルと子要素のまま。画像も背面親だけ。再取得でも変わらず、Dialog内の入力検証ができない。
Popupは別ケースとして、OPEN後の親画像のみ/メニューAXはあるがScreenshot unavailableという差があった。
初回の座標click/scrollはnoWindowsAvailable、fixture再起動後は成功。内部原因は未特定。

**公開できる添付候補:** この最小ソース、上表の再現/対照結果、fixtureだけの画像。
生ログ、アプリ全一覧、ローカル絶対パス、環境識別子、token、ユーザー入力は送らない。
環境版情報と内部ログが必要ならPMが送付範囲を確認してから個別に匿名化する。

## 検証と証拠の保管

- JDKコンパイル、jpackage app-image作成、plain/.app JAR hash一致を確認。
- `./gradlew test`: 114 tests、failures/errors/skipped 0。製品コード/パッケージは変更していないためbuildPlugin対象外。
- buildスクリプトの既存output拒否、無効run ID拒否を実行して確認。既存runは上書きしない。
- screenshot/AX/イベントログ・PID/leaseは非公開run資材として保管。ツール呼出しのエラー/前後はこのタスク履歴にも残る。
  `ax-events.local.json` は採取したAXスナップショットの補助記録で、全API呼出しの完全トレースではない。
- 共有可能なfixture画像のSHA-256（ファイル本体はローカル保管）:
  - modal-parent.png: `98ed4b8d1800939800ad5235e8336db1f9c89c3419d85beeb6e4f6d36589dae8`
  - restart-click.png: `0dfba46f64a42d6cddc5c11430947fb421a87097804dfa1a83bac53302a9156d`
  - restart-scroll.png: `83908c30452c7eea849be8790aebac1fe8b77e19e0da42661bef05df09f21743`

この文書の追加はGUI実行後であり、上記source/JARを新HEADのビルドと偽装しない。
PRのコード差分は固定fixture sourceと同一、文書は実験結果の追記。
独立レビューとCIはPRへ記録する。#80の固定候補での報告整合確認と、#73のCUA操作成立は
別受入であり、develop統合時はどちらのIssueもcloseしない。
