# 正式名称: Cursor in Android Studio（#30）

- 日付: 2026-09-06 / GPT、Computer Use単独操作、Claude Pro独立静的レビュー。
- 対象: [#30](https://github.com/shinma06/cursor-in-android-studio/issues/30)、MV-043。
- ソース: `c5abde4`（実機反映時clean）。`./gradlew test buildPlugin`成功、63 tests / 0 failures / 0 errors。
- 配布物: `cursor-in-android-studio-0.1.0-SNAPSHOT.zip`。
- ZIP SHA-256: `1cefc077f12b82741d6f6a4fb2b0b5984c0c89e5c133cc5355775d5fcc20c03f`。
- メインJAR SHA-256: `13e6f813da62215de2ffe0318103d505e416884f9b69ad4c87ec23275fd334dc`。
- Android Studio `AI-261.26222.65.2614.16204760`。04:19:47に終了、04:20:14に配置、04:20:18の起動ログで `Loaded custom plugins: Cursor in Android Studio (0.1.0-SNAPSHOT)` を確認。

## 変更と互換性

製品表示、Settings名、通知グループ表示、エラーダイアログ名、Gradleプロジェクト/ZIP、現行ガイドを新名称に統一。GitHubリポジトリを `shinma06/cursor-in-android-studio` にrenameしoriginも変更。#1/#30とGit履歴へのアクセスを確認。

`com.cursoragent.plugin`、settings/state/storage、内部のツールウィンドウと通知グループIDは維持。旧インストールはローカル証跡へ退避し、新フォルダに配置して同じplugin IDの重複読込みを防止。作業中のローカルcheckoutパスと過去の証跡/CLI fixtureは維持。

## 実機観察

- ツールウィンドウ見出し・サイドバー名・Agentヘルプ・空状態が `Cursor in Android Studio` になった（AXと画像）。通常幅で空状態の見出しは全文表示。
- 右側のウィンドウ位置とComposer 2.5 / Agent選択が更新後も保持された。
- Settingsメニューを選択しても操作ツールは元のウィンドウだけを返した。Raise、接続初期化、ショートカットでも対象設定画面が取得できず、Settings/Plugins/Notificationsの実画面確認はblocked。設定変更・プロンプト送信は行っていない。
- 製品不具合と断定せず、MV-043の総合判定はblocked。名称変更のコード/配布とGitHub変更は完了。

ローカル証跡: `.loop-runs/20260906-rename/evidence/` の `before.jpg`、`renamed-window.jpg`、`final-window.txt`、`install.json`、`startup.txt`、`claude-review.txt`。保存データの全面的な復元QAを行ったという意味ではない。

## Claudeレビューの評価

1. 旧slugの残存/CI破損の懸念: 全文検索でCIやloop scriptsに旧ZIP名の固定指定がないことを確認。skill概要も新名称へ更新。履歴ファイル名/証跡は意図的に保持。
2. toolWindowのXML `displayName` 提案: 不採用。対象SDKのToolWindowEPに該当属性なし。実装されたinitで新タイトルを確認済み。`ActivateToolWindowActionKt` のローカルSDKコードは表示に `getStripeTitleProvider()` を使うため、IDを変更する必要なし。
3. 定数とresource bundleの混在: 不採用。ブランドは翻訳しない固有名詞。KotlinはPluginBrand.NAME、XMLの通知表示はプラットフォームが要求するbundle/keyを使用。
4. 空状態の長い見出し: 通常の実機パネル幅で全文を画像確認。極小幅は追加QA対象。
5. NotificationGroupEPの属性: `javap -v`で `@Attribute(value="bundle")` と `@Attribute(value="key")` を確認。JAR内のproperties/XMLも検査済み。実画面は上記blocked。

次: 操作ツールでSettings/Plugins画面が取得可能になったらMV-043の残りを確認。#20/#27等の既存受入状態は変更しない。
